import 'package:flutter/material.dart';

class GameEffects extends StatelessWidget {
  const GameEffects({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
