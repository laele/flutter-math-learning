import 'package:flutter/material.dart';

class HomeCompletedGame extends StatelessWidget {
  const HomeCompletedGame({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
